# PDF library execution report

## Final delivery — 2026-09-18, r3

**PASS; local Herd ready for user testing.** Final checkpoint pdf-library-20260918-r3. QA-R2-01 resolved with defect-sensitive browser coverage and directed code/test/design/runtime evidence. Full original QA matrix retained; assigned recovery and causal transport cases re-executed. Architecture conformance passed with all33 hashes matching. Contract-aware completion gate exited0; visible `toscanini verify` exited0 (canonical-r3-pass.md). No product files changed afterward.

Herd https://oceanix-api.test points to this checkout. Applied only the two PDF-library migrations as batch12; migrate:status confirms both Ran. HTTP302 authentication redirect confirms local service responds. Built assets are current and no Vite hot file overrides the manifest. No .env changes, production deployment, commit, push or PR update. Existing unrelated compliance/composer/translations preserved. Authenticated behavioral QA used disposable fixtures, not live Herd training records. Company library abilities are now in the catalog; non-admin access still requires explicit existing profile grants, never automatic broad grants.

Remaining non-blocking observation DES-R3-01: after an interrupted opening the old progress message may remain alongside the working recovery alert. It does not prevent editing, Save or retry; deferred with design/QA evidence. All13 in-contract blockers resolved; no outstanding delivery blocker.

Directional efficiency22/100: elapsed/baseline -45 (includes overnight owner wait), repeated starts -20, additional batch -8, late-round finding -3, follow-up observation -2. This measures workflow cost, not product quality.21 specialist starts within owner-approved21; two correction batches within limit2. QA-R2-01 detected by QA, failure stage implementation; earlier test gaps attributed to tests, review intake to orchestration. Proposed reusable learnings below remain pending and no instructions were changed.

## Historical r2 checkpoint

### Status recorded on 2026-09-17, r2

Delivery BLOCKED by QA-R2-01. Checkpoint `pdf-library-20260916-r2` has 33 production/test hashes. Canonical verification passed: 920 PHP tests, 16 skipped, 5152 assertions; 30 editor units; three actual PDF browser suites and the remaining canonical browser/build/format gates. Evidence: canonical-r2-pass.txt. The previous 12 ledger findings are resolved through deterministic or assigned directed evidence; full r1 code/test/design inspections are retained with directed r2 reports. Independent QA executed all QA-01–08 in a new disposable environment: seven passed; QA-07 failed on interrupted initial modal opening. Its complete report is qa-r2.md. Unsaved HTML remains in memory, but Save and Insert PDF become disabled and offered recovery discards local values. Detector: QA; failure stage: implementation; basis AC-06/INV-04/B-14. No other finding or scope expansion.

The owner authorized the narrow validation-tooling recovery recorded in validation-recovery.md. Eleven focused tooling tests pass. Historical events and product requirements were not changed. The completion gate exits1 for the actual unresolved QA finding, failed QA and missing architecture conformance. No Herd migration, deployment, commit, push or PR update has occurred. The following sections are retained historical r1 status, not the current delivery verdict.

## Pending bounded replan

15 of 17 specialist starts used, one of two correction batches used. Do not dispatch a partial second batch that cannot finish its required verification. Request owner approval to raise the specialist ceiling to21, retain the two-batch limit and unchanged product scope/architecture/AC/INV/QA matrix. At most six further starts: one Worker fixes QA-R2-01 and adds defect-sensitive coverage, directed Code Review and Test Analyst at one stable checkpoint, directed design verification only if its surface changes, directed QA rechecks QA-R2-01 and smallest causal PDF transport regressions while retaining complete original evidence, then the one final Architect conformance. Run deterministic verification once after correction and reopen only affected gates. This proposal is pending, not an approved contract amendment.

Current directional efficiency score53/100: elapsed/baseline -23, repeated starts -14, blocked completion -10; no additional-batch, late-round or out-of-scope deductions. Elapsed telemetry includes owner-wait time and is not a product-quality score. Earlier DET/TA findings are attributed to tests; CR/DES and QA-R2-01 to implementation. QA detected the final missed initial-open boundary as intended. Pending reusable learning: include entry-point requests, not only already-open dialog actions, when planning transport-failure scenarios for stateful editors. No instruction changes applied.

## Historical r1 status

Status: implementation and deterministic verification finished; delivery BLOCKED on independent validation. Not approved for release.

Run `pdf-library-20260916`, frozen scope `pdf-library-v1`. Owner approved the specification/plan/design and architecture hash in specs/007-pdf-library/approval.md. Only owner-scoped PDF listing, private Open, draft Reuse and retained Archive, plus their direct PDF regressions, are included. No deployment, Herd migration, PR update, .env or instruction/tooling change.

## Evidence collected so far

- Contract/readiness preflight passed with no findings before Worker dispatch.
- Worker reports initial integrated PHP checkpoint: 47 PDF tests, 335 assertions, passed. This is an intermediate result, not final checkpoint evidence.
- Existing real browser upload regression and build passed before new browser-library validation.
- B-12 native PostgreSQL two-process probe passed both actual Action orderings, including observed blocking, archive-first refusal, reuse-first retained attachment and actual pending Save. Run-owned database provenance is in postgres-readiness.md; disposable files at /tmp/oceanix-pdf-qa-HhaDkJ. Final review still pending.

## Remaining gates

Final deterministic verification; frozen implementation checkpoint; independent Code Review and Test Analyst; complete executable QA-01–08 and design review; architecture conformance; contract-aware and visible project exit gates.

### Final checkpoint update

`pdf-library-20260916-r1` is recorded in checkpoint-r1.json with 32 source/test file hashes against base bdd2b1718c096e9070a2d7ad155def51a0d4a33e. Hashes remained unchanged through final canonical verification. `toscanini verify --run-id pdf-library-20260916` exited0: 911 PHP tests passed,16 skipped,5059 assertions;30 editor unit tests; both actual PDF browser suites; build and remaining canonical browser/preview/format checks passed. Exact summaries are in canonical-r1-pass.txt.

Initial canonical failure DET-01 was a whole-page greedy HTML assertion exhausting PCRE backtracking after modal expansion. The Worker replaced only that assertion with exact row-token/state/alert/message DOM checks. A mutation removing the expected alert failed correctly; the affected suite passed56/656 and canonical rerun passed. No unrelated product change.

Independent Code Review and Test Analyst stopped at intake because the scenario artifact included later Worker execution outcomes. No implementation review or approval occurred. A neutral behavior-only extraction now exists at review-scenarios.md. Design reviewer launch hit the agent thread limit. QA and architecture conformance have not run.

The contract-aware completion gate exited1. Besides genuinely missing reviews/QA/conformance, it classifies the Worker's second pre-review implementation pass as a repeated whole-scope review and requires replanning; it also demands a specification-reviewer gate. No gate/tooling/instruction or historical telemetry was altered to force approval. Tooling repair is outside the approved scope; owner direction is required before expanding work. Full evidence: review-intake-blocked.md and .toscanini/runtime/runs/pdf-library-20260916/execution-report.md.

No Herd migration, deployment, commit, push or PR update. The code and all evidence are preserved locally.

## Efficiency and attribution

Generated directional score:87/100, deductions: elapsed/baseline -1, repeated specialist start -2, blocked completion -10. This is not a product-quality score. Seven specialist starts; no independent product-review finding exists. DET-01 was detected by canonical verification and attributed to tests; review intake contamination was detected by both independent reviewers and attributed to orchestration. The temporary concurrency-test permission refusal required concrete database provenance/emptiness proof; it was resolved without changing application data or bypassing the review. Detected by execution permission review; failure stage: readiness evidence handoff.

Proposed reusable learning (pending, not applied): include exact disposable resource provenance and read-only ownership/isolation proof in the initial Worker readiness handoff whenever a test must create schema or fixtures in a local database. The owner has not accepted an instruction change.

Additional reusable learning (pending, not applied): provide reviewers approved scenario definitions separately from Worker execution evidence, so scenario traceability does not contaminate independent intake. No project instructions changed.
