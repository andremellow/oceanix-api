# Authorized validation recovery

## 2026-09-18 bounded continuation

The owner authorized the second bounded remediation and local test readiness (remediation-r2-approval.md). The contract validator now accepts an explicit owner replan's exact specialist ceiling with decision/date/evidence/finding IDs; absent or mismatched authorization remains rejected and the remediation-round ceiling is unchanged. The completion gate also retains actually recorded original QA coverage after a valid directed closure, rather than demanding another full QA run. It does not invent missing original scenarios, count unvalidated directed results, or relabel directed execution as independent. Original QA-01–08 and newly executed QA-07 remain separate immutable telemetry evidence. Thirteen tooling unit checks pass, including negative controls for both changes. No historical events or project instructions changed.

On 2026-09-17 the owner explicitly replied “Sim” to correcting only the validation-workflow blocker and finishing validation. Product scope, architecture hash, AC/INV/QA matrix and budgets remain unchanged. This is a narrow mechanical workflow repair, not a new product feature or specification waiver.

Permitted changes: gate classification of actual reviewer roles, explicit owner-approved restart records for blocked intake attempts with no review/findings, and opt-in specification-reviewer requirement consistent with the repository policy (approved specification/clarification/plan remain mandatory). Tests must prove that missing, contaminated, repeated substantive or over-budget reviews still block. Preserve event history and all prior failures; never convert a blocked verdict into approval.

Restart approval applies only to agents pdf_library_code_review and pdf_library_test_review at checkpoint pdf-library-20260916-r1, scope pdf-library-v1. Both stopped at contaminated briefing intake without implementation findings. Their starts still count toward the17 specialist budget. A new independent fresh review is required. Neutral scenario definitions: review-scenarios.md. Worker outcomes and previous review verdicts are excluded from new briefings.

Repair files: .toscanini/bin/toscanini-gate.py and focused .toscanini/tests/test_gate.py only; run metadata/evidence may be updated. No agent instructions, historical telemetry, application functionality or .env changes are included. Root owns this tooling-only edit; no other Worker edits production or tests concurrently. New product corrections, if independent review finds them, remain subject to the original consolidated-remediation rules.

## Implemented checks

The gate now counts only actual independent reviewer roles for repeated whole-scope reviews (Worker starts still count in specialist budgets). Explicit restart approval binds the exact recorded agent/start timestamp/scope/checkpoint and applies only to a blocked intake with zero findings and no completed review. Existing events remain unchanged. Missing/blocked reviews still prevent completion; no approval is fabricated.

An explicit independentReviewRequired=false removes only the extra specification-specialist gate for this contract, consistent with the repository policy; specification, clarification, plan and owner approval remain validated. The QA prerequisite recognizes a valid directed closure after an independent request-changes verdict, requiring its start, correct scope/checkpoint and resolved ledger findings. It cannot treat a blocked intake as that independent review.

Eleven focused unittest checks pass, including negative controls for absent approvals, incorrect restart bindings, substantive prior reviews, missing/invalid directed closure and retained core gates. Initial tests failed before helper implementation. Actual completion gate still reports missing independent QA and architecture conformance, as required; the erroneous Worker-repeat and extra specification-reviewer blockers are gone. No completion claimed.

The design-reviewer role explicitly records a completed inspection with REQUEST_CHANGES as blocked/request-changes. Its original author confirmed that protocol and the inspection, while retaining the report's runtime limits (it is not full executable QA). The gate now permits that exact design-review state, with positive finding count and report, as the prerequisite for a later directed closure. It does not accept intake blocked, failed runs, zero findings, missing evidence or partial QA, and does not approve the original findings: the existing directed start, scope/checkpoint, resolved ledger and final approving verdict remain mandatory. Two new tests failed before this helper and passed afterward. No historical event was changed or replaced. Real completion remains blocked until the separate full QA matrix and architect conformance complete.
