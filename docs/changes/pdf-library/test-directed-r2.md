# PDF library directed test verification

**APPROVE** — TA-01–TA-05 closed for run `pdf-library-20260916`, scope `pdf-library-v1`, checkpoint `pdf-library-20260916-r2`. Phase remediation, round 1; review mode directed; context inherited; `sourceRole=test-analyst`.

Revalidation reason: Assigned test-effectiveness corrections and direct delta.

This verifies the five findings in `test-analysis-clean.md`, their approved AC/INV/B criteria, and causally affected implementation. It is not a new independent audit. The earlier PDF-upload feature's approvals are not treated as this feature's verdict. No tests were executed and no production/test files or finding ledger were edited.

## Evidence identity and limits

Read the approved specification, architecture, design and approval, neutral review contract/scenarios, r2 checkpoint and remediation scope. All **33 checkpoint file hashes match** the current files. The supplied remediation manifest identifies nine changed files; its lack of an exact r1 textual archive is acknowledged. Review used the final assigned test cases and their corresponding permission, persistence, transport and modal methods, without claiming a reconstructed textual delta or reopening unrelated implementation.

## Assigned findings

| Finding | Decision and evidence |
| --- | --- |
| **TA-01 — Independent write capability denials** | **CLOSED.** `LessonDocumentLibraryAccessTest.php`, “enforces independent write grants and runtime prerequisites on actual actions”, calls the real Reuse and Archive Actions successfully before removing one capability from every actor role. The four dataset cases remove Reuse, Archive, View or CoursesUpdate independently. Removal of Reuse leaves Archive working; removal of Archive leaves Reuse working. Incomplete View/Edit prerequisite cases deny the applicable write. Assertions inspect actual destination pivots, archive presence/absence and unchanged HTML, rather than a Gate result alone. The current access service refreshes the actor and these tests reach its real authorization/target boundaries. Existing owner and revocation coverage remains credited. Basis: AC-05, INV-01, B-05, FR-003. |
| **TA-02 — Existing source reuse and contextual retention** | **CLOSED.** `LessonDocumentLibraryTest.php`, “reuses the same bytes in every draft context and saves only explicitly”, now attaches the document to a distinct source lesson before reuse in each of the three contexts. It compares source HTML/pivot, complete document attributes and bytes; destination changes require explicit Save, and each contextual read compares streamed bytes with an unauthorized control. `LessonDocumentArchiveTest.php` adds a published source to successful reuse-before-archive, asserts source/metadata/hash retention and pending Save, and copies/validates the retained association. Its “serves a reused saved destination…” case exercises an assignee's actual contextual response before and after archive, with unrelated-user denial. The disposable browser fixture's selected catalog documents also have source associations; its three-context flows save/reload reused content. Existing lower-level copy and delivery tests are appropriately reused rather than duplicated across every layer. Basis: AC-03/04, INV-02, B-06/08/11. |
| **TA-03 — Archive retry and interrupted transport** | **CLOSED.** The original browser journey now retries a server-failed archive confirmation to successful row removal while comparing editor HTML. The new “interrupted transport…” browser test aborts actual Livewire search and reuse requests, checks retained rows/search/label/HTML and recovers through the visible retry action. For Archive, it performs `route.fetch()` first, holds and aborts the real response after server execution, then retries the retained confirmation. The fixture queries the database and requires exactly one archival row; the browser requires confirmation closure, row removal, recovered focus and unchanged editor HTML. Subsequent reuse proves the captured formatted selection survives these operations and typing remains correctly positioned. This executes the new request-failure hook and manual retry path rather than substituting a successful response or calling only server fault handlers. Basis: AC-04/06, INV-04, B-13/14. |
| **TA-04 — Keyboard, focus, loading and empty states** | **CLOSED.** Browser evidence now activates Open, Reuse, search/clear/retry, pagination and archive controls using focus plus Enter. Nested confirmation asserts initial Cancel focus, keyboard Cancel/Escape, return to its row and ability to reopen; postarchive focus is required on a surviving row. The narrow Portuguese confirmation checks initial localized Cancel focus, viewport reachability of the confirm action, and Escape dismissal. A separate owner with no PDFs renders the genuine empty state, distinct from the existing populated-library no-results scenario. A held real pagination request requires prior rows to remain, the loading announcement and `aria-busy=true`, and disabled Next, followed by successful page completion and keyboard return. These checks exercise the actual Flux/Livewire state and the corrected modal behavior. Basis: AC-01/06, INV-04, B-02/10/14/15 and the approved design. |
| **TA-05 — Exact caret and persisted plain suffix** | **CLOSED.** The three-context library loop explicitly branches on `Node.ELEMENT_NODE`, inspecting the selected element itself or a text node's parent before `closest('a')`. Both selected-text and custom/default branches require exact prefix, collapsed selection, focused editor and no enclosing anchor. They type a suffix, reject suffix text inside any anchor, and compare the exact paragraph after Save/reload. Custom/default checks retain the ordinary link's exact label. These assertions fail for the reported false-negative case where an element-node caret was inside an anchor, and for continuation at an unrelated final anchor. No insertion stub replaces the real reuse path. Basis: AC-03, INV-04, B-07. |

The original attribution remains: each closed finding was detected by `test-analyst`, with upstream `failureStage=tests`. No new findings or product decisions are introduced.

## Retained coverage and causal regression assessment

Retain the original library audit's credited Eloquent/Action, pagination/literal-search, ownership, stale/published, immutable/idempotent archival, pending-Save and bounded PostgreSQL evidence. The assigned corrections strengthen write isolation, representative source associations and frontend recovery; they do not justify another concurrency matrix or another full audit. The PostgreSQL probe was not rerun or independently reapproved here.

The affected request hook, confirmation error handling and focus behavior were checked against the new transport/keyboard tests. Existing held reuse completion followed by cancellation remains in the browser suite, as do the expanded modal's upload regressions. No new causally introduced test-effectiveness blocker was identified within the assigned scope.

This verdict concerns test effectiveness at the hash-verified r2 source checkpoint. It does not claim a test run, independent QA, canonical verification or overall delivery approval. Those gates remain separately recorded by the orchestrator.

Assigned closures: **TA-01, TA-02, TA-03, TA-04, TA-05**. Unresolved assigned findings: **0**. New findings: **0**. **APPROVE**.
